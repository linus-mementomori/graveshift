
# Buy Me a Coffee username — the `data-id` from their widget snippet.
# Optional: defaults to "projectremus". Unset it and the widget still loads
# with that default; change it here if the username ever changes.
NEXT_PUBLIC_BMC_USERNAME=projectremus
