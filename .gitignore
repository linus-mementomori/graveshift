node_modules/
.next/
next/   # stray build output, committed before the rule above existed
out/
.DS_Store

# Secrets — never commit. See .env.example for the shape.
.env
.env.local
.env*.local

*.tsbuildinfo
next-env.d.ts
